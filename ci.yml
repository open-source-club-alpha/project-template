name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Replace with your actual setup step, e.g.:
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: 20

      - name: Install dependencies
        run: echo "TODO: install command"

      - name: Lint
        run: echo "TODO: lint command"

      - name: Test
        run: echo "TODO: test command"
